import React from 'react';
import {Text, View} from 'react-native';
import Container from '../../components/common/Container';
const Contacts = () => {
  return (
    <Container style={{color: 'red', padding: 100}}>
      <Text>Hi from contacts</Text>
    </Container>
  );
};

export default Contacts;
